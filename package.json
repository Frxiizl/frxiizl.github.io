{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "echo 'Deployment files ready in dist/'",
    "preview": "vite preview"
  },
  "devDependencies": {
	"vite": "^7.0.0",
    "terser": "^5.46.1"
  }
}
